<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_first_blue"
    tools:context=".presentation.SplashActivity">

    <ImageView
        android:id="@+id/splash_logo_imageView"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginTop="140dp"
        android:background="@drawable/ic_logo_300"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/splash_logo_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/splash_logo"
        android:textColor="@color/background_white"
        android:textSize="48sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/splash_logo_imageView"
        app:layout_constraintStart_toStartOf="@+id/splash_logo_imageView"
        app:layout_constraintTop_toBottomOf="@id/splash_logo_imageView" />

</androidx.constraintlayout.widget.ConstraintLayout>